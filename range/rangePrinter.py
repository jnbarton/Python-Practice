class RangePrinter(object):
    """Print a range object."""

    def print_range(range):
        if range != None:
            for i in range:
                print(i)
        else:
            print("Range object is empty.")
